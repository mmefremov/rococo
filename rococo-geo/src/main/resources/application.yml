server:
  port: 8093

grpc:
  server:
    port: 8094

spring:
  application:
    name: rococo-geo
  datasource:
    url: 'jdbc:postgresql://localhost:6432/rococo_geo'
    username: postgres
    password: secret
    hikari:
      connection-timeout: 20000
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 10000
      max-lifetime: 1800000
      auto-commit: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration/rococo-geo
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false
    open-in-view: false

logging:
  level:
    root: INFO
    io.efremov.rococo: DEBUG
---
spring:
  config:
    activate:
      on-profile: 'docker'
  datasource:
    url: 'jdbc:postgresql://rococo-postgres:5432/rococo_geo'
